<h1> This will show all current listings for so users can browse. Possible filters on here for listings by platform, price, publisher? </h1>

<% @listings.each do |listing| %>

    Listing Name: <%= link_to listing.name, listing_path(listing) %>
    Genre: <%= listing.genre %>
    Asking Price: <%= listing.price %>
    Platform: <%= listing.platform %>
    Publisher: <%= listing.publisher %><br>
    
<%end%>

<h2>This works and needs to be styled  </h2>